language: cpp
compiler:
  - gcc
  - clang
before_script:
  - mkdir ../build
  - cd ../build
script:
  - cmake ../mp2-lab2
  - make -j
  - cd ./test
  - ./test_boilerplatelib
notifications:
  email: false
